{
  "name": "biggerthan",
  "private": true,
  "license": "SEE LICENSE IN LICENSE",
  "packageManager": "pnpm@12.4.2",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter deck build && pnpm --filter web build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "simulate": "tsx packages/deck/src/simulate.ts",
    "deploy": "pnpm build && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260915.1",
    "@types/node": "^22.20.2",
    "eslint": "^10.10.0",
    "prettier": "^3.9.6",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1",
    "wrangler": "^4.132.0"
  }
}
