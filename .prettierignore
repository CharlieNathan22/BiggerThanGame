# Prettier also reads .gitignore; these are the extra paths it must not touch.
pnpm-lock.yaml
**/dist/
**/.astro/
**/.wrangler/

# Private deck submodule: formatting belongs in its own repo.
packages/deck/data/

# Generated reports — rewritten by every deck build, never hand-edited.
packages/deck/viability.md
packages/deck/simulation.md
